# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.
Not using Claude Code? Rename this file `AI.md` or `AGENTS.md` and adapt the conventions below for your AI tool of choice — the patterns are AI-agnostic.

# {{PROJECT_NAME}}

## What this is

{{TODO: One paragraph describing the project, its purpose, and its primary users.}}

## Tech stack

{{TODO: List primary languages, frameworks, databases, and infrastructure.
Example:
- Runtime: Node.js 22, TypeScript strict
- API: Fastify 5 + Zod validation
- Database: PostgreSQL 18 + Prisma ORM
- Frontend: Next.js 14 (app router) + MUI Material v7
- Mobile: React Native (Expo SDK 55)
- CI/CD: GitHub Actions, Docker Compose
}}

## Monorepo structure (if applicable)

{{TODO: If this is a monorepo, list each package and its responsibility.
If single-repo, remove this section.
Example:
packages/
- api/       HTTP server - responds to client requests
- worker/    Background jobs - acts on schedule
- web/       Guardian dashboard (Next.js)
- mobile/    Mobile app (React Native / Expo)
- shared/    TypeScript types shared across packages
}}

## Setup and common commands

{{TODO: Replace with project-specific commands.}}

```bash
# Install dependencies
{{install command}}

# Start development
{{dev command}}

# Run all tests
{{test command}}

# Run a single test file
{{single test command}}

# Lint and typecheck
{{lint command}}
{{typecheck command}}

# Build
{{build command}}
```

## Hard constraints - apply everywhere

### File length
**Maximum 200 lines per file.** If a file approaches this limit, split it.

### TypeScript (if applicable)
- Strict mode enabled - do not override
- No `any` - use `unknown` and narrow, or define the type
- No `// @ts-ignore` - fix the type error

### Naming conventions
| Thing | Convention | Example |
|---|---|---|
| Variables, functions | camelCase | `getUserById` |
| Types, interfaces, classes | PascalCase | `UserProfile` |
| Files | kebab-case | `get-user.ts` |
| Database tables | snake_case | `user_profiles` |
| Environment variables | SCREAMING_SNAKE_CASE | `DATABASE_URL` |

### Typography
- **No em dashes or en dashes** anywhere: code, docs, comments, strings. Hyphens only.
- For numeric ranges use a hyphen with no spaces: "1-10", "22:00-07:00".

### Error handling
- No floating promises
- HTTP errors return `{ error: string, code: string }` - never expose stack traces

### Dependencies
- Verify any new dependency is actively maintained (published within 12 months) and commonly used (>10K npm weekly downloads) before adding
- Run `npm audit` / `pnpm audit` before merging any PR that changes dependencies

## Architecture notes

{{TODO: Describe key data flows, service boundaries, external integrations.
Example:
- Packages communicate through the database, not HTTP
- api/ writes rows on request; worker/ reads them on cron schedules
- SOS dispatch must complete atomically (transaction) before returning 200
}}

## Branch strategy

{{TODO: Describe branching model.
Example:
- `develop` is the working branch - all code goes here directly
- `main` is production (releases only)
- No feature branches - work directly on develop
- Run lint + typecheck + unit tests before pushing
}}

## Agents and commands

Auto-discovered from `.claude/agents/` and `.claude/commands/`.

Key agents:
- **`ticket-gate`** - mandatory pre-implementation gate (10/10 from all specialists required)

Run `/upgrade-audit` to compare this project against the claude-scaffold reference kit
and see what governance pieces are missing or outdated.
